#!/bin/bash

set -eEo pipefail
source "$(dirname "$0")/foundationos-env"

if [[ -f $FOUNDATIONOS_PATH/version ]]; then
  cat "$FOUNDATIONOS_PATH/version"
elif [[ -f $FOUNDATIONOS_ROOT/version ]]; then
  cat "$FOUNDATIONOS_ROOT/version"
else
  echo "${FOUNDATIONOS_RELEASE_ID:-dev}"
fi
